{{- if and (.Values.database.postgresql.enabled) (.Values.database.postgresql.persistence.enabled) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "tachydromos.fullname" . }}-postgresql
  labels:
    {{- include "tachydromos.labels" . | nindent 4 }}
  {{- with .Values.database.postgresql.persistence.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  storageClassName: {{ .Values.database.postgresql.persistence.storageClassName }}
  accessModes:
    {{- range .Values.database.postgresql.persistence.accessModes }}
    - {{ . | quote }}
    {{- end }}
  resources:
    requests:
      storage: {{ .Values.database.postgresql.persistence.size }}
{{- end }}
